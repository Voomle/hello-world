const express = require('express');\n// const functions = require('firebase-functions'); // Uncomment for Firebase Cloud Functions deployment\n\nconst app = express();\nconst port = process.env.PORT || 3001;\n\n// Middleware\napp.use(express.json()); // For parsing application/json\n\n// Basic route\napp.get('/api', (req, res) => {\n  res.json({ message: 'Welcome to VoomDico Backend API!' });\n});\n\n// Placeholder for word/phrase routes\napp.get('/api/words', (req, res) => {\n  // TODO: Fetch words from database\n  res.json([]); \n});\n\napp.post('/api/words', (req, res) => {\n  // TODO: Add new word to database\n  const newWord = req.body;\n  console.log('Received new word:', newWord);\n  res.status(201).json({ message: 'Word added successfully (placeholder)', data: newWord });\n});\n\n// Start server only if not in a serverless environment (like Firebase Functions)\nif (process.env.NODE_ENV !== 'production' || !process.env.FUNCTION_TARGET) { \n  app.listen(port, () => {\n    console.log(`Backend server listening on http://localhost:${port}`);\n  });\n}\n\n// Export the app for Firebase Cloud Functions if needed\n// exports.api = functions.https.onRequest(app);
